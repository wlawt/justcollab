const express = require("express");
const router = express.Router();

/* 
    @route      POST api/client/
    @desc       Update b/w client and server
    @access     Public 
*/

router.post("/", (req, res) => {});

module.exports = router;

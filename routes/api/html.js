const express = require("express");
const router = express.Router();

/* 
    @route      GET api/html/
    @desc       Gets most up-to-date website
    @access     Public 
*/

router.get("/", (req, res) => {});

module.exports = router;

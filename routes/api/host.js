const express = require("express");
const router = express.Router();

/* 
    @route      POST api/host/
    @desc       Update the diff b/w host and server
    @access     Public 
*/

router.post("/", (req, res) => {});

module.exports = router;
